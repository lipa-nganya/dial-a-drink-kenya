# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Specific routes
/offers    /index.html   200
/menu      /index.html   200
/cart      /index.html   200
# /admin/* → handled in SPA by RedirectToAdminApp (browser redirect to admin-frontend host)

# Fallback for any other routes
/*    /index.html   200
