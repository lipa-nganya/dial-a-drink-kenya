# Local Development Environment Configuration
# Copy this file to .env.local (which is gitignored) for local development
# DO NOT commit .env.local to git - it contains sensitive credentials

# Node Environment - MUST be 'development' for local
# DO NOT set to 'production' - this will cause local to use production configs
NODE_ENV=development

# Database Configuration (Local PostgreSQL)
# Use local database for development, NOT production Cloud SQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dialadrink
DB_USER=postgres
DB_PASSWORD=password
# DO NOT set DATABASE_URL for local development - it will override local config

# M-Pesa API Credentials (Sandbox for local testing)
# These can be inherited from .env or set here
# MPESA_CONSUMER_KEY=your_sandbox_consumer_key
# MPESA_CONSUMER_SECRET=your_sandbox_consumer_secret
# MPESA_SHORTCODE=174379
# MPESA_PASSKEY=your_sandbox_passkey
MPESA_ENVIRONMENT=sandbox

# M-Pesa Callback URL (ngrok for local development)
# Update this with your ngrok URL when running ngrok
MPESA_CALLBACK_URL=https://your-ngrok-url.ngrok-free.dev/api/mpesa/callback
NGROK_URL=https://your-ngrok-url.ngrok-free.dev

# Frontend URLs (Local)
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001

# Database SSL (Local doesn't need SSL)
DB_REQUIRE_SSL=false

# Google Maps API Key (Optional - can inherit from .env)
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# SMTP Settings (Optional for local)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=your_email@gmail.com
