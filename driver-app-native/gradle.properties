# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
android.defaults.buildfeatures.buildconfig=true

# Gradle JDK configuration
# Use Embedded JDK from Android Studio
org.gradle.java.home=/Applications/Android Studio.app/Contents/jbr/Contents/Home

# API Configuration for different build flavors
# Override these in local.properties for custom URLs

# Local API (ngrok - for local development)
# Override this in local.properties for local testing with ngrok
# For dev builds, this will default to DEV_API_BASE_URL
# Current ngrok URL (update when ngrok restarts):
LOCAL_API_BASE_URL=https://homiest-psychopharmacologic-anaya.ngrok-free.dev

# Development API (GCP backend)
DEV_API_BASE_URL=https://deliveryos-backend-p6bkgryxqa-uc.a.run.app

# Production API (GCP backend)
PROD_API_BASE_URL=https://deliveryos-backend-p6bkgryxqa-uc.a.run.app

