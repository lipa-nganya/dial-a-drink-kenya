<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state (must come first) -->
    <item android:state_focused="true" android:color="#4CAF50" />
    <!-- Enabled state -->
    <item android:state_enabled="true" android:color="#4CAF50" />
    <!-- Default state (must come last) -->
    <item android:color="#4CAF50" />
</selector>
